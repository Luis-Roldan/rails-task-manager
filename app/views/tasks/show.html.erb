<h1>
  <%= @task.title %>
</h1>
<div>
  <%= @task.details %>
</div>
<div>
  <% if @task.completed %>
    <p>
      <%= "Task completed" %>
    </p>
    <% else %>
    <p>
      <%= "Task not completed yet" %>
    </p>
  <% end %>
</div>
<%= link_to "Back to list", tasks_path  %>
